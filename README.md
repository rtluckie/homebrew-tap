# homebrew-tap

Homebrew tap for releases from multiple projects.

Each project lives under its own namespace in `Formula/`, e.g. `Formula/rtluckie/vizid/vizid.rb`.

## Install

```
brew tap rtluckie/tap
brew install gvizid
```
